# Taller B5-T1 - Generacion de Datos Financieros Sinteticos
# Pensado para ejecutarse en Google Colab (TensorFlow ya viene preinstalado) o
# en local una vez resuelto el problema de "long paths" de Windows con pip
# (ver README, seccion "Entorno").

numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.8
duckdb>=1.0
pyarrow>=14.0
tensorflow>=2.16
jupyter>=1.0
tqdm>=4.66
